import { PaletteMode } from "@mui/material";

const light = {
  palette: {
    mode: "light" as PaletteMode,
  },
};

const dark = {
  palette: {
    mode: "dark" as PaletteMode,
  },
};

export {
  light,
  dark
}